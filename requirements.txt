# No external dependencies needed.
# rf_inference.py uses only the Python standard library (json, os) --
# this avoids scikit-learn/scipy, which previously pushed the deployed
# function bundle over Vercel's 225MB serverless function size limit.
